# Supabase project URL, e.g. https://abcdefgh.supabase.co
SUPABASE_URL=
# Supabase service/secret key (sb_secret_... or service_role JWT)
SUPABASE_KEY=
# Identifies which student the events belong to
STUDENT_ID=
# Optional overrides
DAEMON_PORT=5699
FLUSH_INTERVAL_SECS=60
SUPABASE_TABLE=unified_events
# Used elsewhere in the project
ANTHROPIC_API_KEY=
